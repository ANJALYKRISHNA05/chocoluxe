<%- include('../partials/user/header') %>

<div class="container my-5">
  <div class="row">
    <!-- Filter Sidebar -->
    <div class="col-lg-3 mb-4">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
          <h5 class="mb-0">Filters</h5>
        </div>
        <div class="card-body">
          <form id="filterForm" action="/products" method="GET">
            <!-- Search Box -->
            <div class="mb-3">
              <label for="search" class="form-label">Search Products</label>
              <div class="input-group">
                <input type="text" class="form-control" name="search" id="search" value="<%= filters.search || '' %>" placeholder="Search products...">
                <% if (filters.search) { %>
                  <button type="button" class="btn btn-outline-secondary" onclick="clearSearch()">
                    <i class="fas fa-times"></i>
                  </button>
                <% } %>
              </div>
            </div>

            <!-- Sort Options -->
            <div class="mb-3">
              <label for="sort" class="form-label">Sort By</label>
              <select class="form-select" name="sort" id="sort" onchange="this.form.submit()">
                <option value="createdAt" <%= !filters.sort || filters.sort === 'createdAt' ? 'selected' : '' %>>Newest First</option>
                <option value="price_low_high" <%= filters.sort === 'price_low_high' ? 'selected' : '' %>>Price: Low to High</option>
                <option value="price_high_low" <%= filters.sort === 'price_high_low' ? 'selected' : '' %>>Price: High to Low</option>
                <option value="name_asc" <%= filters.sort === 'name_asc' ? 'selected' : '' %>>Name: A to Z</option>
                <option value="name_desc" <%= filters.sort === 'name_desc' ? 'selected' : '' %>>Name: Z to A</option>
              </select>
            </div>

            <hr>

            <!-- Category Filter -->
            <div class="mb-3">
              <label class="form-label">Category</label>
              <div class="overflow-auto" style="max-height: 150px;">
                <% categories.forEach(category => { %>
                  <div class="form-check">
                    <input class="form-check-input filter-checkbox" type="radio" name="category" 
                      id="category<%= category._id %>" value="<%= category._id %>" 
                      <%= filters.category === category._id.toString() ? 'checked' : '' %>>
                    <label class="form-check-label" for="category<%= category._id %>">
                      <%= category.name %>
                    </label>
                  </div>
                <% }); %>
              </div>
            </div>

            <hr>

            <!-- Flavor Filter -->
            <div class="mb-3">
              <label class="form-label">Flavor</label>
              <% flavors.forEach(flavor => { %>
                <div class="form-check">
                  <input class="form-check-input filter-checkbox" type="radio" name="flavor" 
                    id="flavor<%= flavor %>" value="<%= flavor %>" 
                    <%= filters.flavor === flavor ? 'checked' : '' %>>
                  <label class="form-check-label" for="flavor<%= flavor %>">
                    <%= flavor %>
                  </label>
                </div>
              <% }); %>
            </div>

            <hr>

            <!-- Sugar Level Filter -->
            <div class="mb-3">
              <label class="form-label">Sugar Level</label>
              <% sugarLevels.forEach(level => { %>
                <div class="form-check">
                  <input class="form-check-input filter-checkbox" type="radio" name="sugarLevel" 
                    id="sugar<%= level %>" value="<%= level %>" 
                    <%= filters.sugarLevel === level ? 'checked' : '' %>>
                  <label class="form-check-label" for="sugar<%= level %>">
                    <%= level %>
                  </label>
                </div>
              <% }); %>
            </div>

            <hr>

            <!-- Weight Filter -->
            <div class="mb-3">
              <label class="form-label">Weight (grams)</label>
              <% weights.forEach(weight => { %>
                <div class="form-check">
                  <input class="form-check-input filter-checkbox" type="radio" name="weight" 
                    id="weight<%= weight %>" value="<%= weight %>" 
                    <%= filters.weight === weight.toString() ? 'checked' : '' %>>
                  <label class="form-check-label" for="weight<%= weight %>">
                    <%= weight %>g
                  </label>
                </div>
              <% }); %>
            </div>

            <hr>

            <!-- Price Range Filter -->
            <div class="mb-3">
              <label class="form-label">Price Range</label>
              <div class="d-flex gap-2">
                <div class="flex-grow-1">
                  <input type="number" class="form-control" name="minPrice" placeholder="Min" 
                    value="<%= filters.minPrice || '' %>" min="0">
                </div>
                <div class="flex-grow-1">
                  <input type="number" class="form-control" name="maxPrice" placeholder="Max" 
                    value="<%= filters.maxPrice || '' %>" min="0">
                </div>
              </div>
            </div>

            <div class="d-grid gap-2 mt-3">
              <button type="submit" class="btn btn-primary">Apply Filters</button>
              <button type="button" class="btn btn-outline-secondary" onclick="clearAllFilters()">Clear All</button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- Product Listing -->
    <div class="col-lg-9">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Products</h2>
        <p><%= products.length %> products found</p>
      </div>

      <% if (products.length === 0) { %>
        <div class="alert alert-info">
          No products found matching your criteria. Try adjusting your filters.
        </div>
      <% } else { %>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
          <% products.forEach(product => { %>
            <div class="col">
              <div class="card h-100 shadow-sm product-card">
                <a href="/product/<%= product._id %>" class="text-decoration-none text-dark">
                  <!-- Product Image - Using the pre-selected displayVariant -->
                  <img src="<%= product.displayVariant.productImage[0] %>" class="card-img-top" alt="<%= product.productName %>" style="height: 200px; object-fit: cover;">
                  
                  <div class="card-body">
                    <!-- Product Name -->
                    <h5 class="card-title"><%= product.productName %></h5>
                    
                    <!-- Category -->
                    <p class="card-text text-muted mb-1">
                      <small><%= product.category.name %></small>
                    </p>
                    
                    <!-- Variant Details - Using the pre-selected displayVariant -->
                    <p class="card-text mb-1">
                      <span class="badge bg-light text-dark"><%= product.displayVariant.flavor %></span>
                      <span class="badge bg-light text-dark"><%= product.displayVariant.sugarLevel %></span>
                      <span class="badge bg-light text-dark"><%= product.displayVariant.weight %>g</span>
                    </p>
                    
                    <!-- Pricing - Using the pre-selected displayVariant -->
                    <div class="d-flex align-items-center mt-2">
                      <% if (product.displayVariant.salePrice < product.displayVariant.regularPrice) { %>
                        <h5 class="m-0 text-danger">₹<%= product.displayVariant.salePrice.toFixed(2) %></h5>
                        <span class="text-muted ms-2"><del>₹<%= product.displayVariant.regularPrice.toFixed(2) %></del></span>
                      <% } else { %>
                        <h5 class="m-0">₹<%= product.displayVariant.regularPrice.toFixed(2) %></h5>
                      <% } %>
                    </div>
                    
                    <!-- Stock Status - Using the pre-selected displayVariant -->
                    <p class="card-text mt-2">
                      <% if (product.displayVariant.stock_quantity > 0) { %>
                        <span class="badge bg-success">In Stock</span>
                      <% } else { %>
                        <span class="badge bg-danger">Out of Stock</span>
                      <% } %>
                    </p>
                  </div>
                </a>
              </div>
            </div>
          <% }); %>
        </div>
      <% } %>
    </div>
  </div>
</div>

<script>
  // Function to apply filters when checkboxes are clicked
  document.querySelectorAll('.filter-checkbox').forEach(checkbox => {
    checkbox.addEventListener('change', function() {
      document.getElementById('filterForm').submit();
    });
  });
  
  // Function to clear search
  function clearSearch() {
    const searchInput = document.getElementById('search');
    searchInput.value = '';
    document.getElementById('filterForm').submit();
  }
  
  // Function to clear all filters
  function clearAllFilters() {
    window.location.href = '/products';
  }
</script>

<%- include('../partials/user/footer') %>
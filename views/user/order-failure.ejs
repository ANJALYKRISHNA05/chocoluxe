<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Order Failed - CHOCOLUXE</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
  <style>
   
    body {
      font-family: 'Nunito Sans', sans-serif;
      background-color: #fcf9f7;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }
    
    .failure-section {
      flex: 1;
      padding: 3rem 0;
    }
    
   
    .text-center.mb-5 {
      margin-bottom: 3rem !important;
    }
    
    .failure-icon {
      font-size: 5rem;
      color: #dc3545;
      margin-bottom: 1.5rem;
      display: inline-block;
      background: linear-gradient(145deg, #fff, #f8f3ef);
      border-radius: 50%;
      padding: 1.5rem;
      box-shadow: 0 10px 25px rgba(220, 53, 69, 0.15);
    }
    
    .text-center h2 {
      font-family: 'Playfair Display', serif;
      color: #33261d;
      font-weight: 700;
      margin-bottom: 1rem;
      font-size: 2.5rem;
    }
    
    .text-center p {
      color: #5d534c;
      font-size: 1.1rem;
      margin-bottom: 1.5rem;
    }
    
    /* Card Styling */
    .failure-card {
      border: none;
      border-radius: 12px;
      padding: 2rem;
      background: linear-gradient(145deg, #fff, #f8f3ef);
      margin-bottom: 2rem;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .failure-card:hover {
      box-shadow: 0 12px 25px rgba(139, 90, 43, 0.15);
    }
    
   
    .btn-primary {
      background-color: #8b5a2b;
      border-color: #8b5a2b;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      transition: all 0.3s ease;
      margin-bottom: 1rem;
    }
    
    .btn-primary:hover {
      background-color: #6d4621;
      border-color: #6d4621;
      transform: translateY(-3px);
      box-shadow: 0 8px 15px rgba(139, 90, 43, 0.25);
    }
    
    .btn-outline-dark {
      border-color: #8b5a2b;
      color: #8b5a2b;
      font-weight: 600;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      transition: all 0.3s ease;
    }
    
    .btn-outline-dark:hover {
      background-color: #8b5a2b;
      border-color: #8b5a2b;
      color: #fff;
      transform: translateY(-3px);
      box-shadow: 0 8px 15px rgba(139, 90, 43, 0.15);
    }
    
   
    .action-buttons {
      margin-top: 2rem;
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }
    
  
    @media (max-width: 768px) {
      .failure-section {
        padding: 2rem 0;
      }
      
      .text-center h2 {
        font-size: 2rem;
      }
      
      .failure-icon {
        font-size: 4rem;
        padding: 1.25rem;
      }
      
      .failure-card {
        padding: 1.5rem;
      }
    }
    
    @media (max-width: 576px) {
      .failure-section {
        padding: 1.5rem 0;
      }
      
      .text-center h2 {
        font-size: 1.75rem;
      }
      
      .failure-card {
        padding: 1.25rem;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/user/header') %>

  <section class="failure-section py-5">
    <div class="container">
      <div class="failure-card">
        <div class="text-center mb-4">
          <i class="fas fa-times-circle failure-icon mb-3"></i>
          <h2>Payment Failed</h2>
          <p>We're sorry, but there was an issue processing your payment.</p>
          <p class="text-muted">
            <%= errorMessage || "Your payment could not be completed. Please try again or choose a different payment method." %>
          </p>
        </div>

        <div class="action-buttons">
          <% if (orderId) { %>
            <a href="/checkout/retry-payment/<%= orderId %>" class="btn btn-primary btn-lg w-100">Retry Payment</a>
          <% } else { %>
            <a href="/checkout" class="btn btn-primary btn-lg w-100">Return to Checkout</a>
          <% } %>
          <a href="/products" class="btn btn-outline-dark btn-lg w-100">Continue Shopping</a>
        </div>
      </div>
    </div>
  </section>

  <%- include('../partials/user/footer') %>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

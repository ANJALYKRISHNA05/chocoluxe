<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CHOCOLUXE - Sales Report</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Nunito+Sans:wght@300;400;600;700;800;900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        body {
            font-family: 'Nunito Sans', sans-serif;
            margin: 0;
            background-color: #f5e8d4;
            overflow-x: hidden;
        }

        
        .content {
            margin-left: 250px;
            padding: 20px;
            margin-top: 60px;
            min-height: calc(100vh - 60px);
            transition: margin-left 0.3s ease;
        }

    
        .card {
            background-color: #fff9e6;
            border: none;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 20px;
            transition: transform 0.2s ease;
        }

        .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.15);
        }

        .card-title {
            color: #4a2c2a;
            font-size: 1.2rem;
            font-weight: 600;
            margin-bottom: 10px;
        }

        .card-value {
            color: #4a2c2a;
            font-size: 1.8rem;
            font-weight: 700;
        }

        .filter-card {
            background-color: #fff9e6;
            border: none;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            padding: 15px;
            margin-bottom: 20px;
        }

        .btn-chocolate {
            background-color: #4a2c2a;
            color: #fff;
            border: none;
            transition: all 0.3s ease;
        }

        .btn-chocolate:hover {
            background-color: #3a1c1a;
            color: #fff;
            transform: translateY(-1px);
        }

        .btn-outline-chocolate {
            color: #4a2c2a;
            border-color: #4a2c2a;
            background-color: transparent;
        }

        .btn-outline-chocolate:hover {
            background-color: #4a2c2a;
            color: #fff;
        }

    
        .table {
            background-color: #fff9e6;
            border-radius: 10px;
            overflow: hidden;
        }

        .table th, .table td {
            color: #4a2c2a;
            vertical-align: middle;
            padding: 12px;
        }

        .table thead th {
            background-color: #d1a876;
            color: #fff;
            border: none;
            font-weight: 600;
        }

        .table-striped tbody tr:nth-of-type(odd) {
            background-color: rgba(209, 168, 118, 0.1);
        }

    
        .chart-container {
            position: relative;
            height: 300px;
            width: 100%;
        }

        .date-range-picker {
            border: 1px solid #d1a876;
            border-radius: 5px;
            padding: 8px 12px;
            background-color: #fff;
            color: #4a2c2a;
        }

        .download-btn {
            margin-right: 10px;
            margin-bottom: 10px;
        }

       
        .alert-light {
            background-color: #fff9e6;
            border-color: #d1a876;
            color: #4a2c2a;
        }

        
        @media (max-width: 1200px) {
            .content {
                margin-left: 220px;
                padding: 15px;
            }
            
            .card-value {
                font-size: 1.6rem;
            }
            
            .chart-container {
                height: 280px;
            }
        }


        @media (max-width: 992px) {
            .content {
                margin-left: 200px;
                padding: 15px;
            }
            
            .card-value {
                font-size: 1.5rem;
            }
            
            .chart-container {
                height: 250px;
            }
            
            .table th, .table td {
                padding: 8px;
                font-size: 0.9rem;
            }
        }

       
        @media (max-width: 768px) {
            .content {
                margin-left: 70px;
                padding: 10px;
            }
            
            .card {
                padding: 15px;
                margin-bottom: 15px;
            }
            
            .card-title {
                font-size: 1rem;
            }
            
            .card-value {
                font-size: 1.4rem;
            }
            
            .chart-container {
                height: 220px;
            }
            
            .filter-card {
                padding: 10px;
            }
            
        
            .filter-form .col-md-3 {
                margin-bottom: 10px;
            }
            
          
            .download-section {
                flex-direction: column;
                gap: 10px;
            }
            
            .download-btn {
                width: 100%;
                margin-right: 0;
            }
            
         
            .table th, .table td {
                padding: 6px;
                font-size: 0.85rem;
            }
            
           
            .table .d-none-mobile {
                display: none;
            }
            
          
            .pagination {
                flex-wrap: wrap;
                justify-content: center;
            }
            
            .pagination .page-item {
                margin: 2px;
            }
        }

    
        @media (max-width: 576px) {
            .content {
                margin-left: 60px;
                padding: 8px;
            }
            
            h2 {
                font-size: 1.5rem;
                text-align: center;
                margin-bottom: 20px;
            }
            
            .card {
                padding: 12px;
                margin-bottom: 12px;
            }
            
            .card-title {
                font-size: 0.9rem;
                margin-bottom: 8px;
            }
            
            .card-value {
                font-size: 1.2rem;
            }
            
            .chart-container {
                height: 200px;
            }
            
          
            .metrics-row .col-md-3 {
                flex: 0 0 50%;
                max-width: 50%;
            }
            
            .charts-row .col-md-6,
            .charts-row .col-md-4 {
                flex: 0 0 100%;
                max-width: 100%;
                margin-bottom: 15px;
            }
            
          
            .filter-form .col-md-3 {
                flex: 0 0 100%;
                max-width: 100%;
            }
            
          
            .table-responsive {
                border-radius: 10px;
                overflow-x: auto;
            }
            
            .table {
                min-width: 600px;
            }
            
            .table th, .table td {
                padding: 4px;
                font-size: 0.8rem;
                white-space: nowrap;
            }
            
        
            .alert {
                font-size: 0.9rem;
                padding: 10px;
            }
            
          
            .pagination .page-link {
                padding: 0.25rem 0.5rem;
                font-size: 0.8rem;
            }
        }

      
        @media (max-width: 400px) {
            .content {
                margin-left: 50px;
                padding: 5px;
            }
            
            .card {
                padding: 10px;
            }
            
            .card-title {
                font-size: 0.85rem;
            }
            
            .card-value {
                font-size: 1.1rem;
            }
            
            .chart-container {
                height: 180px;
            }
            
          
            .metrics-row .col-md-3 {
                flex: 0 0 100%;
                max-width: 100%;
            }
            
            .btn {
                font-size: 0.8rem;
                padding: 0.5rem 0.75rem;
            }
            
            h2 {
                font-size: 1.3rem;
            }
        }

        
        @media (max-height: 500px) and (orientation: landscape) {
            .content {
                margin-top: 50px;
                padding: 10px;
            }
            
            .chart-container {
                height: 150px;
            }
            
            .card {
                padding: 10px;
                margin-bottom: 10px;
            }
        }

        @media print {
            .content {
                margin-left: 0;
                margin-top: 0;
            }
            
            .btn, .pagination {
                display: none;
            }
            
            .card {
                break-inside: avoid;
                box-shadow: none;
                border: 1px solid #ddd;
            }
        }

        .text-responsive {
            font-size: calc(0.8rem + 0.5vw);
        }

        .card-responsive {
            min-height: auto;
        }

        @media (max-width: 768px) {
            .mb-4 {
                margin-bottom: 1rem !important;
            }
            
            .py-4 {
                padding-top: 1rem !important;
                padding-bottom: 1rem !important;
            }
        }
    </style>
</head>
<body>

    <%- include('../partials/admin/header') %>

    <%- include('../partials/admin/sidebar', { activePage: 'sales-report' }) %>

    <div class="content">
        <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap">
            <h2 style="color: #4a2c2a;" class="mb-2 mb-md-0">Sales Report</h2>
        </div>

        <div class="filter-card mb-4">
            <form action="/admin/sales-report" method="GET" class="row g-3 align-items-end filter-form">
                <div class="col-md-3">
                    <label for="filter" class="form-label">Report Type</label>
                    <select name="filter" id="filter" class="form-select" onchange="toggleDateFields()">
                        <option value="daily" <%= filterType === 'daily' ? 'selected' : '' %>>Daily</option>
                        <option value="weekly" <%= filterType === 'weekly' ? 'selected' : '' %>>Weekly</option>
                        <option value="monthly" <%= filterType === 'monthly' ? 'selected' : '' %>>Monthly</option>
                        <option value="yearly" <%= filterType === 'yearly' ? 'selected' : '' %>>Yearly</option>
                        <option value="custom" <%= filterType === 'custom' ? 'selected' : '' %>>Custom Date Range</option>
                    </select>
                </div>
                <div class="col-md-3 custom-date-field" style="<%= filterType !== 'custom' ? 'display: none;' : '' %>">
                    <label for="startDate" class="form-label">Start Date</label>
                    <input type="date" name="startDate" id="startDate" class="form-control date-range-picker" value="<%= filterType === 'custom' ? dateRange.startDate.toISOString().split('T')[0] : '' %>" />
                </div>
                <div class="col-md-3 custom-date-field" style="<%= filterType !== 'custom' ? 'display: none;' : '' %>">
                    <label for="endDate" class="form-label">End Date</label>
                    <input type="date" name="endDate" id="endDate" class="form-control date-range-picker" value="<%= filterType === 'custom' ? dateRange.endDate.toISOString().split('T')[0] : '' %>" />
                </div>
                <div class="col-md-3">
                    <button type="submit" class="btn btn-chocolate w-100">Apply Filter</button>
                </div>
            </form>
        </div>

        <div class="alert alert-light mb-4">
            <strong>Report Period:</strong> 
            <%= dateRange.startDate.toLocaleDateString() %> to <%= dateRange.endDate.toLocaleDateString() %>
        </div>

        <div class="row mb-4 metrics-row">
            <div class="col-md-3 col-6">
                <div class="card card-responsive">
                    <h5 class="card-title">Total Orders</h5>
                    <p class="card-value"><%= salesMetrics.orderCount %></p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="card card-responsive">
                    <h5 class="card-title">Total Revenue</h5>
                    <p class="card-value">₹<%= salesMetrics.totalSales.toFixed(2) %></p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="card card-responsive">
                    <h5 class="card-title">Total Discount</h5>
                    <p class="card-value">₹<%= salesMetrics.totalDiscount.toFixed(2) %></p>
                </div>
            </div>
            <div class="col-md-3 col-6">
                <div class="card card-responsive">
                    <h5 class="card-title">Gross Revenue</h5>
                    <p class="card-value">₹<%= salesMetrics.grossRevenue.toFixed(2) %></p>
                </div>
            </div>
        </div>

     
        <div class="row mb-4 charts-row">
            <div class="col-md-6">
                <div class="card">
                    <h5 class="card-title">Sales Overview</h5>
                    <div class="chart-container">
                        <canvas id="salesOverviewChart"></canvas>
                    </div>
                </div>
            </div>
            
            <div class="col-md-6">
                <div class="card">
                    <h5 class="card-title">Payment Methods</h5>
                    <div class="chart-container">
                        <canvas id="paymentMethodsChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

      
        <div class="row mb-4 charts-row">
            <div class="col-md-4">
                <div class="card">
                    <h5 class="card-title">Top Products</h5>
                    <div class="chart-container">
                        <canvas id="topProductsChart"></canvas>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card">
                    <h5 class="card-title">Top Categories</h5>
                    <div class="chart-container">
                        <canvas id="topCategoriesChart"></canvas>
                    </div>
                </div>
            </div>
            
            <div class="col-md-4">
                <div class="card">
                    <h5 class="card-title">Top Coupons</h5>
                    <div class="chart-container">
                        <canvas id="topCouponsChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

     
        <div class="card mb-4">
            <h5 class="card-title">Best Selling Products (Top 5)</h5>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Product Name</th>
                            <th>Total Sales Amount</th>
                            <th class="d-none-mobile">Quantity Sold</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (topProducts && topProducts.length > 0) { %>
                            <% topProducts.forEach((product, index) => { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= product.productDetails.productName %></td>
                                    <td>₹<%= product.totalRevenue.toFixed(2) %></td>
                                    <td class="d-none-mobile"><%= product.totalQuantity %></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="text-center">No product sales data available for this period</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        
        <div class="card mb-4">
            <h5 class="card-title">Best Selling Categories (Top 5)</h5>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Category Name</th>
                            <th>Total Sales Amount</th>
                            <th class="d-none-mobile">Total Orders</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (topCategories && topCategories.length > 0) { %>
                            <% topCategories.forEach((category, index) => { %>
                                <tr>
                                    <td><%= index + 1 %></td>
                                    <td><%= category.categoryName %></td>
                                    <td>₹<%= category.totalRevenue.toFixed(2) %></td>
                                    <td class="d-none-mobile"><%= category.orderCount %></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="text-center">No category sales data available for this period</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>

        <div class="card mb-4">
            <h5 class="card-title">Download Report</h5>
            <div class="d-flex justify-content-center gap-2 py-4 download-section flex-wrap">
                <a href="/admin/sales-report/download/pdf?filter=<%= filterType %>&startDate=<%= dateRange.startDate.toISOString().split('T')[0] %>&endDate=<%= dateRange.endDate.toISOString().split('T')[0] %>" class="btn btn-danger download-btn">
                    <i class="fas fa-file-pdf me-2"></i> <span class="d-none d-sm-inline">Download as </span>PDF
                </a>
                <a href="/admin/sales-report/download/excel?filter=<%= filterType %>&startDate=<%= dateRange.startDate.toISOString().split('T')[0] %>&endDate=<%= dateRange.endDate.toISOString().split('T')[0] %>" class="btn btn-success download-btn">
                    <i class="fas fa-file-excel me-2"></i> <span class="d-none d-sm-inline">Download as </span>Excel
                </a>
            </div>
        </div>

        <div class="card mb-4">
            <h5 class="card-title">Transaction Details</h5>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Order ID</th>
                            <th>Date</th>
                            <th class="d-none-mobile">Customer</th>
                            <th class="d-none-mobile">Payment Method</th>
                            <th>Status</th>
                            <th class="d-none-mobile">Discount</th>
                            <th>Total</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (orders && orders.length > 0) { %>
                            <% orders.slice(pagination.skip, pagination.skip + pagination.limit).forEach(order => { %>
                                <tr>
                                    <td><%= order.orderId %></td>
                                    <td><%= new Date(order.createdAt).toLocaleDateString() %></td>
                                    <td class="d-none-mobile"><%= order.shippingAddress.name %></td>
                                    <td class="d-none-mobile"><%= order.paymentMethod %></td>
                                    <td>
                                        <span class="badge <%= order.status === 'Delivered' ? 'bg-success' : 
                                                          order.status === 'Cancelled' ? 'bg-danger' : 
                                                          order.status === 'Returned' ? 'bg-warning' : 'bg-info' %>">
                                            <%= order.status %>
                                        </span>
                                    </td>
                                    <td class="d-none-mobile">₹<%= order.discount.toFixed(2) %></td>
                                    <td>₹<%= order.total.toFixed(2) %></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="7" class="text-center">No transactions found for this period</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>

          
            <% if (orders && orders.length > 0) { %>
                <div class="d-flex justify-content-between align-items-center px-3 py-2 flex-wrap">
                    <div class="mb-2 mb-md-0">
                        <small>Showing <%= Math.min(orders.length, pagination.skip + 1) %> to <%= Math.min(orders.length, pagination.skip + pagination.limit) %> of <%= orders.length %> transactions</small>
                    </div>
                    <nav aria-label="Transaction pagination">
                        <ul class="pagination mb-0">
                            <li class="page-item <%= pagination.page <= 1 ? 'disabled' : '' %>">
                                <a class="page-link" href="/admin/sales-report?filter=<%= filterType %>&startDate=<%= dateRange.startDate.toISOString().split('T')[0] %>&endDate=<%= dateRange.endDate.toISOString().split('T')[0] %>&page=<%= pagination.page - 1 %>" aria-label="Previous">
                                    <span aria-hidden="true">&laquo;</span>
                                </a>
                            </li>
                            <% for(let i = 1; i <= pagination.totalPages; i++) { %>
                                <li class="page-item <%= pagination.page === i ? 'active' : '' %>">
                                    <a class="page-link" href="/admin/sales-report?filter=<%= filterType %>&startDate=<%= dateRange.startDate.toISOString().split('T')[0] %>&endDate=<%= dateRange.endDate.toISOString().split('T')[0] %>&page=<%= i %>"><%= i %></a>
                                </li>
                            <% } %>
                            <li class="page-item <%= pagination.page >= pagination.totalPages ? 'disabled' : '' %>">
                                <a class="page-link" href="/admin/sales-report?filter=<%= filterType %>&startDate=<%= dateRange.startDate.toISOString().split('T')[0] %>&endDate=<%= dateRange.endDate.toISOString().split('T')[0] %>&page=<%= pagination.page + 1 %>" aria-label="Next">
                                    <span aria-hidden="true">&raquo;</span>
                                </a>
                            </li>
                        </ul>
                    </nav>
                </div>
            <% } %>
        </div>
    </div>

    <%- include('../partials/admin/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script>
        function toggleDateFields() {
            const filterType = document.getElementById('filter').value;
            const customDateFields = document.querySelectorAll('.custom-date-field');
            
            if (filterType === 'custom') {
                customDateFields.forEach(field => field.style.display = 'block');
            } else {
                customDateFields.forEach(field => field.style.display = 'none');
            }
        }

        document.addEventListener('DOMContentLoaded', function() {
        
            Chart.defaults.responsive = true;
            Chart.defaults.maintainAspectRatio = false;
          
            const salesOverviewCtx = document.getElementById('salesOverviewChart').getContext('2d');
            new Chart(salesOverviewCtx, {
                type: 'bar',
                data: {
                    labels: ['Revenue', 'Discount', 'Savings'],
                    datasets: [{
                        label: 'Amount (₹)',
                        data: [<%= salesMetrics.totalSales %>, <%= salesMetrics.totalDiscount %>, <%= salesMetrics.totalSavings %>],
                        backgroundColor: [
                            'rgba(75, 192, 192, 0.6)',
                            'rgba(255, 99, 132, 0.6)',
                            'rgba(255, 159, 64, 0.6)'
                        ],
                        borderColor: [
                            'rgba(75, 192, 192, 1)',
                            'rgba(255, 99, 132, 1)',
                            'rgba(255, 159, 64, 1)'
                        ],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true
                        }
                    },
                    plugins: {
                        legend: {
                            display: window.innerWidth > 576
                        }
                    }
                }
            });

           
            const paymentMethodsCtx = document.getElementById('paymentMethodsChart').getContext('2d');
            const paymentMethodNames = <%- JSON.stringify(Object.keys(paymentMethods)) %>;
            const paymentCounts = <%- JSON.stringify(Object.values(paymentMethods).map(p => p.count)) %>;
            
            new Chart(paymentMethodsCtx, {
                type: 'pie',
                data: {
                    labels: paymentMethodNames,
                    datasets: [{
                        data: paymentCounts,
                        backgroundColor: [
                            'rgba(54, 162, 235, 0.6)',
                            'rgba(255, 99, 132, 0.6)',
                            'rgba(255, 206, 86, 0.6)',
                            'rgba(75, 192, 192, 0.6)',
                            'rgba(153, 102, 255, 0.6)'
                        ],
                        borderColor: [
                            'rgba(54, 162, 235, 1)',
                            'rgba(255, 99, 132, 1)',
                            'rgba(255, 206, 86, 1)',
                            'rgba(75, 192, 192, 1)',
                            'rgba(153, 102, 255, 1)'
                        ],
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: window.innerWidth > 768 ? 'right' : 'bottom',
                            labels: {
                                boxWidth: window.innerWidth > 576 ? 12 : 8,
                                font: {
                                    size: window.innerWidth > 576 ? 12 : 10
                                }
                            }
                        }
                    }
                }
            });

            <% if (topProducts && topProducts.length > 0) { %>
                const topProductsCtx = document.getElementById('topProductsChart').getContext('2d');
                const productNames = <%- JSON.stringify(topProducts.map(p => p.productDetails.productName.substring(0, 15) + (p.productDetails.productName.length > 15 ? '...' : ''))) %>;
                const productRevenue = <%- JSON.stringify(topProducts.map(p => p.totalRevenue)) %>;
                
                new Chart(topProductsCtx, {
                    type: 'bar',
                    data: {
                        labels: productNames,
                        datasets: [{
                            label: 'Revenue (₹)',
                            data: productRevenue,
                            backgroundColor: 'rgba(75, 192, 192, 0.6)',
                            borderColor: 'rgba(75, 192, 192, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: {
                            y: {
                                beginAtZero: true
                            },
                            x: {
                                ticks: {
                                    maxRotation: window.innerWidth > 576 ? 45 : 90,
                                    font: {
                                        size: window.innerWidth > 576 ? 10 : 8
                                    }
                                }
                            }
                        },
                        plugins: {
                            legend: {
                                display: window.innerWidth > 576
                            }
                        }
                    }
                });
            <% } %>

            
            <% if (topCategories && topCategories.length > 0) { %>
                const topCategoriesCtx = document.getElementById('topCategoriesChart').getContext('2d');
                const categoryNames = <%- JSON.stringify(topCategories.map(c => c.categoryName)) %>;
                const categoryRevenue = <%- JSON.stringify(topCategories.map(c => c.totalRevenue)) %>;
                
                new Chart(topCategoriesCtx, {
                    type: 'doughnut',
                    data: {
                        labels: categoryNames,
                        datasets: [{
                            data: categoryRevenue,
                            backgroundColor: [
                                'rgba(255, 99, 132, 0.6)',
                                'rgba(54, 162, 235, 0.6)',
                                'rgba(255, 206, 86, 0.6)',
                                'rgba(75, 192, 192, 0.6)',
                                'rgba(153, 102, 255, 0.6)'
                            ],
                            borderColor: [
                                'rgba(255, 99, 132, 1)',
                                'rgba(54, 162, 235, 1)',
                                'rgba(255, 206, 86, 1)',
                                'rgba(75, 192, 192, 1)',
                                'rgba(153, 102, 255, 1)'
                            ],
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                position: window.innerWidth > 768 ? 'right' : 'bottom',
                                labels: {
                                    boxWidth: window.innerWidth > 576 ? 12 : 8,
                                    font: {
                                        size: window.innerWidth > 576 ? 12 : 10
                                    }
                                }
                            }
                        }
                    }
                });
            <% } %>

         
            <% if (topCoupons && topCoupons.length > 0) { %>
                const topCouponsCtx = document.getElementById('topCouponsChart').getContext('2d');
                const couponCodes = <%- JSON.stringify(topCoupons.map(c => c.couponDetails.code)) %>;
                const couponDiscount = <%- JSON.stringify(topCoupons.map(c => c.totalDiscount)) %>;
                
                new Chart(topCouponsCtx, {
                    type: 'bar',
                    data: {
                        labels: couponCodes,
                        datasets: [{
                            label: 'Discount (₹)',
                            data: couponDiscount,
                            backgroundColor: 'rgba(255, 159, 64, 0.6)',
                            borderColor: 'rgba(255, 159, 64, 1)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        scales: {
                            y: {
                                beginAtZero: true
                            },
                            x: {
                                ticks: {
                                    maxRotation: window.innerWidth > 576 ? 45 : 90,
                                    font: {
                                        size: window.innerWidth > 576 ? 10 : 8
                                    }
                                }
                            }
                        },
                        plugins: {
                            legend: {
                                display: window.innerWidth > 576
                            }
                        }
                    }
                });
            <% } %>

          
            window.addEventListener('resize', function() {
                Chart.helpers.each(Chart.instances, function(instance) {
                    instance.resize();
                });
            });
        });
    </script>
</body>
</html>